---
# name·description은 두 하네스(Claude Code·Codex)가 모두 읽는다 — 크로스 하네스 계약.
# 트리거·동작에 필요한 정보는 전부 이 둘에 담는다.
name: <kebab-case-이름, 디렉토리명과 일치>
description: >
  <무엇을 하는 스킬인지 한두 문장.>
  <언제 발동해야 하는지 — 구체적 사용자 표현을 여러 개 나열.>
  사용자가 "<트리거1>", "<트리거2>", "<트리거3>" 같은 표현을 쓰면 반드시 이 스킬을 사용한다.
  <경쟁·혼동 가능한 다른 스킬이 있으면 경계를 적는다: "~는 이 스킬이 아니라 XXX가 다룬다".>
# 아래 3개는 Claude Code 전용 필드다. Codex는 모르는 필드로 무시하고 name·description만 쓴다.
# 유지해도 되지만(Codex에 무해), 스킬 동작이 이 필드에 의존하면 안 된다.
allowed-tools: <이 스킬이 쓰는 도구, 쉼표 구분. 불필요하면 이 줄 삭제>
argument-hint: (선택) <인자 설명>. 생략 시 <기본 동작>
# model: <opus 등, 특정 모델이 필요할 때만>
---

# <스킬 이름>

<이 스킬이 무엇을 하는지, 왜 필요한지 2~3문장. "언제 쓰나"는 여기 쓰지 않는다 — description에.>

**핵심 원칙**:
- <이 스킬이 지키는 판단 기준 1 — 왜 중요한지 함께>
- <원칙 2>

---

## 실행 절차

### 1. <첫 단계>

<명령형으로. 왜 이 단계가 필요한지 짧게.>

### 2. <다음 단계>

<...>

### 3. <마지막 단계>

<출력 형식이 있으면 정확한 템플릿을 박는다:>

```
<출력 템플릿>
```

---

## 행동 원칙

- <실행 중 지켜야 할 것 1>
- <자주 하는 실수와 그걸 피하는 법>

---

<!--
분량 가이드
- 이 본문은 <500줄. 넘으면 references/<주제>.md로 넘기고 "여기부터 X를 읽어라"로 링크.
- 여러 도메인/프레임워크 지원 시: references/<변형>.md로 쪼개고 본문은 선택 로직만.
- 반복·결정론적 작업은 scripts/에 코드로. 산출물 템플릿은 assets/에.
- 결정론적으로 검증 가능하면 evals/<이름>.py 테스트 케이스를 만들고 build-skill 러너로 돌린다.

Codex 호환
- name·description만이 두 하네스 공통이다. 트리거 표현, 스킬이 하는 일, 경계를 전부 여기 담는다.
- allowed-tools·argument-hint·model은 Claude 전용 — Codex는 무시한다. 넣어도 무해하나 의존 금지.
- 본문이 Claude 전용 도구(EnterPlanMode, 구조화 질문 도구, 서브에이전트, Skill 호출 등)를
  전제하면, 그 도구가 없는 하네스용 폴백을 문장으로 적는다.
  예: "계획 모드 도구가 있으면 그 흐름을 쓰고, 없으면 계획서를 파일로 저장하고 대화로 승인받는다."
  예: "서브에이전트가 있으면 병렬 조사, 없으면 순차로 직접 조사한다."
- 특정 하네스에서 아예 성립 불가한 스킬이면, 감지해서 이유를 알리고 종료하도록 본문에 적는다
  (skill-metrics 패턴).
- bash·파일 조작만으로 되는 스킬은 대체로 그대로 이식된다 — 되도록 그렇게 설계한다.
-->
